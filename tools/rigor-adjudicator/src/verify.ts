import { sha256Json } from './io.js';
export interface VerifyResult { ok:boolean; errors:string[]; exit_code:0|2|3; }
export function verifyArtifacts(a:any):VerifyResult { const e:string[]=[]; if(!a.packet) e.push('missing evidence packet'); if(!a.deterministic) e.push('missing deterministic evaluation'); if(a.jev){ if(a.jev.model_id!=='typesafe/jev-1.13') e.push('wrong model id'); if(a.jev.rubric_version!=='v1') e.push('wrong or mixed rubric version'); if(a.packet && a.jev.packet_sha256!==sha256Json(a.packet)) e.push('packet hash mismatch'); } if(a.deterministic?.promotion_blocked&&a.adjudication?.promotion_eligible)e.push('promotion eligible despite hard-rule block'); if((a.adjudication?.disagreements??[]).some((d:any)=>d.consequential&&d.resolution_status!=='RESOLVED'))e.push('unresolved consequential disagreement'); const structural=e.some(x=>x.startsWith('missing ')); return {ok:e.length===0,errors:e,exit_code:e.length?(structural?2:3):0}; }
