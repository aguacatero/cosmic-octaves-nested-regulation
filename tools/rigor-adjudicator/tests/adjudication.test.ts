import assert from 'node:assert/strict'; import { test } from 'node:test'; import { adjudicate } from '../src/adjudication.js'; import { validPacket } from './fixtures/packet.js'; import { runDeterministicRules } from '../src/deterministic-rules.js';
test('hard-rule fail beats Jev 0.99 pass',()=>{const p=structuredClone(validPacket); p.data.discovery_status='inspected'; p.data.confirmation_status='untouched'; const d=runDeterministicRules(p,{requested_maturity:'CONFIRMATION-READY'}); const r=adjudicate({packet:p,deterministic:d,jev:{schema_version:'1.0',packet_id:p.packet_id,model_id:'typesafe/jev-1.13',rubric_version:'v1',packet_sha256:'x',status:'OK',promotion_eligible:false,criteria:[{criterion_id:'confirmation_data_identified',status:'PASS',probability:0.99,answer:true}],provider_metadata:{}},authority_status:'ADVISORY_ONLY',requested_maturity:'CONFIRMATION-READY'}); assert.equal(r.gate_status,'FAIL'); assert.equal(r.promotion_eligible,false);});
test('uncalibrated Jev cannot create authoritative PASS',()=>{const d=runDeterministicRules(validPacket,{}); const r=adjudicate({packet:validPacket,deterministic:d,jev:null,authority_status:'ADVISORY_ONLY'}); assert.notEqual(r.gate_status,'PASS');});
