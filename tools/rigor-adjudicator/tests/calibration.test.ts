import assert from 'node:assert/strict'; import { test } from 'node:test'; import { calculateCalibration } from '../src/calibration.js';
test('calculates hand-checkable Brier and false pass',()=>{const rows=[{rubric_id:'G6',label:1 as const,probability:0.9},{rubric_id:'G6',label:1 as const,probability:0.6},{rubric_id:'G6',label:0 as const,probability:0.4},{rubric_id:'G6',label:0 as const,probability:0.1}]; const r=calculateCalibration(rows,{pass:0.8,human_reviewed_case_count:0}); assert.ok(Math.abs(r.brier_score-0.085)<1e-9); assert.equal(r.false_pass_rate,0); assert.equal(r.authority_status,'ADVISORY_ONLY');});
test('refuses calibrated authority below 150 human cases',()=>{const rows=[{rubric_id:'G0',label:1 as const,probability:0.9}]; assert.throws(()=>calculateCalibration(rows,{pass:0.8,human_reviewed_case_count:149,request_approval:true}));});
test('Plan 1 cannot self-approve even with 150 human-reviewed cases',()=>{const rows=[{rubric_id:'G0',label:1 as const,probability:0.9}]; assert.throws(()=>calculateCalibration(rows,{pass:0.8,human_reviewed_case_count:150,request_approval:true}),/Plan 1 is advisory-only/);});
