import assert from 'node:assert/strict'; import { test } from 'node:test'; import { verifyArtifacts } from '../src/verify.js'; import { sha256Json } from '../src/io.js';
test('verifier rejects wrong model id',()=>{const r=verifyArtifacts({packet:{packet_id:'x'},jev:{model_id:'typesafe/other',packet_sha256:'x'},deterministic:{promotion_blocked:false,rules:{}},adjudication:{promotion_eligible:false,disagreements:[]}} as any); assert.equal(r.ok,false);});
test('verifier rejects promotion when hard rule blocks',()=>{const r=verifyArtifacts({packet:{packet_id:'x'},jev:null,deterministic:{promotion_blocked:true,rules:{}},adjudication:{promotion_eligible:true,disagreements:[]}} as any); assert.equal(r.ok,false);});
test('verifier rejects packet hash mismatch',()=>{const packet={packet_id:'x'}; const r=verifyArtifacts({packet,jev:{model_id:'typesafe/jev-1.13',rubric_version:'v1',packet_sha256:'wrong'},deterministic:{promotion_blocked:false,rules:{}},adjudication:{promotion_eligible:false,disagreements:[]}} as any); assert.equal(r.ok,false); assert.match(r.errors.join(' '),/hash mismatch/);});
test('verifier rejects wrong rubric version',()=>{const packet={packet_id:'x'}; const r=verifyArtifacts({packet,jev:{model_id:'typesafe/jev-1.13',rubric_version:'v2',packet_sha256:sha256Json(packet)},deterministic:{promotion_blocked:false,rules:{}},adjudication:{promotion_eligible:false,disagreements:[]}} as any); assert.equal(r.ok,false); assert.match(r.errors.join(' '),/rubric version/);});
test('verifier rejects missing deterministic evaluation',()=>{const r=verifyArtifacts({packet:{packet_id:'x'},adjudication:{promotion_eligible:false,disagreements:[]}} as any); assert.equal(r.exit_code,2);});
