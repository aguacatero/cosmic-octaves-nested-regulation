import assert from 'node:assert/strict'; import { test } from 'node:test'; import { buildDecisionRequest, createJevClient } from '../src/jev-client.js'; import { validPacket } from './fixtures/packet.js'; import { loadRubric } from '../src/rubrics.js';
test('uses pinned Jev model and Decisions request shape',async()=>{let seen:any; const c=createJevClient({transport:async(req:any)=>{seen=req; return {answers:{canonical_dataset_identified:{answer:true,probability:0.9}},id:'r1'};}}); await c.decide(buildDecisionRequest(validPacket,[loadRubric('G6')])); assert.equal(seen.decisionsRequest.model,'typesafe/jev-1.13'); assert.equal(seen.decisionsRequest.state.records[0].id,'X-1');});
